"""Shared test fixtures for ZoneGuard backend tests."""

import sys
import os

# Ensure backend root is in path for imports
sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))
